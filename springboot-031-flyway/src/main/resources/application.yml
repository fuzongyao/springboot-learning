spring:
  datasource:
    url: jdbc:mysql://localhost:3307/common?characterEncoding=utf8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  flyway.clean-disabled: true

mybatis-plus:
  global-config:
    db-config:
      dbType: mysql